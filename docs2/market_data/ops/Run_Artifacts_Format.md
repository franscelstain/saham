# Run Artifacts Format

Generate after finalize:
- run summary JSON (counts + hashes + config snapshot)
- eligibility CSV
- failures CSV (from eod_fetch_failures)
- anomaly report
No secrets in artifacts.