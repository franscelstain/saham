# Policy: NO_TRADE (SOP)
## Reason codes & severity

- Definisi `severity_level` dan outcome class (EXCLUDE/NOT_QUALIFIED/WARN/INFO) adalah **LOCKED** di `docs/watchlist/1.contract.md`.
- Reason codes yang **shared** lintas policy ada di `docs/watchlist/5.reasons_catalog.md`.


> **Source of Truth (LOCKED)**
> Dokumen policy ini mengunci **Hard Rules, Soft Rules, Risk Rules, PLAN (EOD-only), dan Scoring weights** untuk policy ini.
> - Universe filter + grouping + recommendations allocation ada di `docs/watchlist/2.watchlist.md` (global).
> - CONFIRM checks ada di `4.scorecard.md`.

Dokumen ini hanya mendefinisikan aturan spesifik policy. Aturan universal wajib lihat `docs/watchlist/2.watchlist.md` bagian **Global Contract**.

Kontrak lintas-policy ada di `docs/watchlist/2.watchlist.md`.

---

## 0) Tujuan
Menonaktifkan NEW ENTRY secara deterministik ketika kondisi “bukan saatnya entry”.

---

## 1) Prinsip
- NO_TRADE adalah policy yang dipilih (aktif), bukan side effect.
- Saat aktif:
  - `groups.top_picks=[]`
  - `groups.secondary=[]`
  - `recommendations=[]`
  - `groups.watch_only` dan `groups.avoid` boleh diisi untuk monitoring

---

## Hard triggers (LOCKED)
`NO_TRADE` **tidak punya trigger otomatis**. Policy ini adalah **manual off-switch** yang dipilih user.

Jika canonical EOD belum ready, itu **bukan** alasan memakai `NO_TRADE`; kondisi tersebut ditangani oleh Global Contract (`EOD_NOT_READY`):
- `recommendations=[]` wajib
- groups boleh monitoring dengan flag `EOD_NOT_READY`

## Mini strategy (Top Picks & Secondary) (EOD-only) (LOCKED)

Tidak berlaku untuk NO_TRADE.

## Invalidation & monitoring (EOD-only)
- NO_TRADE adalah policy manual-only: tidak ada entry, tidak ada recommendations.

## CONFIRM hints (intraday, non-binding)
CONFIRM hanya boleh approve/reject/adjust timing; tidak boleh mengubah PLAN.

- Jika user memilih NO_TRADE, CONFIRM tidak relevan (selalu no entry).
